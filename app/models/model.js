'use strict'

class Model {
  constructor (cache) {
    this.cache = cache
    this.initData(this.cache)
  }

  private initData (cache) {
    if()
  }

  public fetchData (cache)
}

module.exports = Model
