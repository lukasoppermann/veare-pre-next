{{#if (eq section.titleType "subtitle")}}
  <h4 class="Section__title Section__title--subtitle {{classes}}">{{section.title}}</h4>
{{/if}}

{{#if (eq section.titleType "headline")}}
  <h2 class="Section__title Section__title--headline {{classes}}">{{section.title}}</h2>
{{/if}}
