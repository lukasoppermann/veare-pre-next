{{#if (eq item.titleType "subtitle")}}
  <h4 class="Section__title Section__title--subtitle {{classes}}">{{item.title}}</h4>
{{/if}}

{{#if (eq item.titleType "headline")}}
  <h2 class="Section__title Section__title--headline {{classes}}">{{item.title}}</h2>
{{/if}}
