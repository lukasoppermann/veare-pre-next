version: '2'
services:
    veare:
        restart: always
        image: veare/veare:latest
        container_name: veare
        volumes:
            - ../:/usr/src/app
        networks:
            - docker_appnet
# used for interal communication within docker
networks:
    docker_appnet:
        external: true
